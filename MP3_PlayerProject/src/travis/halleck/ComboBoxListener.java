package travis.halleck;

/**
 * 
 * @author Travis Halleck
 * Used only when user moves and deletes a song file which
 * will trigger this method that is attached to my combo box
 *
 */

public interface ComboBoxListener {
	void removeItem(String item);
}
